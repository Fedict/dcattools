PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE 
	{ 	?s dcterms:description ?title }
INSERT
	{ 	?s dcterms:language ?str }
WHERE
	{ 	?s dcterms:language ?title .
		BIND(REPLACE(?title, "^<p>(.*)</p>$", "\1") as ?str) .
		FILTER (STRSTARTS(?title, "<p>"))
	}