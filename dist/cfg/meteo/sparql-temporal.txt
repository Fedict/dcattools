PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE
	{	?dataset dcterms:temporal ?str }
INSERT
	{ 	?dataset dcterms:temporal ?temp }
WHERE
	{ 	?dataset dcterms:temporal ?str
		BIND (REPLACE(REPLACE(?str, "\\n *", " "), 
					"T[0-2][0-9]:[0-5][0-9]:[0-5][0-9]", "" ) as ?temp)
	}