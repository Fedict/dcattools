PREFIX dcat: <http://www.w3.org/ns/dcat#>
PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE
	{	?s ?p ?o }
INSERT
	{ 	?news ?p ?newo }
WHERE
	{ 	?s ?p ?o .
		BIND ( STR(?s) as ?strs )
		BIND ( STR(?o) as ?stro )
		BIND ( "http://opendata.meteo.be/catalog:8080/geonetwork/resource/" as ?old )
		BIND ( "http://data.gov.be/dataset/meteo/" as ?new )
		BIND ( IRI(REPLACE(?strs, ?old, ?new)) as ?news )
		BIND ( IF(isIRI(?o),
				IRI(REPLACE(?stro, ?old, ?new)),
				?o) as ?newo )
		FILTER ( STRSTARTS(?strs, ?old) || STRSTARTS(?stro, ?old) )
	}