PREFIX dcat: <http://www.w3.org/ns/dcat#>

DELETE 
	{ ?s ?prop ?val }
INSERT 
	{ ?s ?prop ?iri }
WHERE 
	{ ?s ?prop ?val .
		VALUES ?prop  { dcat:accessURL dcat:downloadURL }
		BIND (IRI(ENCODE_FOR_URI(?val)) as ?iri)
		FILTER ISLITERAL(?val) 
	}