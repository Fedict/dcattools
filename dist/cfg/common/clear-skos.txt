PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

DELETE { ?s ?p ?o }
WHERE { 
		{	?s ?p ?o 
			VALUES (?p) {
				( skos:prefLabel )
				( skos:altLabel )
				( skos:exactMatch )
				( skos:closeMatch )
				( skos:inScheme )
			}
		} UNION {
			?s a skos:Concept
		}
	}