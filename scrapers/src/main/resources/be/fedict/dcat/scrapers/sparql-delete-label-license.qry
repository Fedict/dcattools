# Remove licenses that are literals when there already is another with a IRI

PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE 
	{	?dist dcterms:license ?label }
WHERE
	{	?dist dcterms:license ?label .
		?dist dcterms:license ?iri .
		FILTER (isLiteral(?label) && isIRI(?iri))
	}