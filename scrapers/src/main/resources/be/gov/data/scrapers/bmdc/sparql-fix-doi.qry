# Check for DOI ID

PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE {
		?s dcterms:identifier ?id }
INSERT {
		?s dcterms:identifier ?doi }
WHERE
	{	?s dcterms:identifier ?id .
		BIND (
			CONCAT("https://doi.org/", ?id)
			AS ?doi)
		FILTER (STRSTARTS(?id, "10.24417"))
	}