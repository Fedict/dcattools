# Change SKOS RDFS label to prefLabel

PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>

DELETE 
	{	?s rdfs:label ?label }
INSERT 
	{ 	?s skos:prefLabel ?label }
WHERE
	{	?s a skos:Concept .
		?s rdfs:label ?label .
		FILTER NOT EXISTS { ?s skos:prefLabel ?pref }
	}
