# Remove empty literals

DELETE 
	{	?s ?p ?label }
WHERE
	{	?s ?p ?label .
		BIND (STR(?label) AS ?lit)
		FILTER (isLiteral(?lit) && (
					(STRLEN(?lit) = 0) || REGEX(?lit, "^[ ?-]*$") || ?lit = "\u00A0" ) 
		)
	}