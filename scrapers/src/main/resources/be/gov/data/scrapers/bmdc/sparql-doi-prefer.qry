# Prefer DOI if present

PREFIX dcterms: <http://purl.org/dc/terms/> 

DELETE {
		?s dcterms:identifier ?id }
INSERT {
		?s dcterms:identifier ?doi }
WHERE
	{	?s dcterms:identifier ?doi .
		?s dcterms:identifier ?id .
		FILTER (STRSTARTS(?doi, "doi:") && (?doi != ?id))
	}
