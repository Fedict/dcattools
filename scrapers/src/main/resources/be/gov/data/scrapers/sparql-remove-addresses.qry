# Remove physical addresses, we're not really interested in those

PREFIX locn: <http://www.w3.org/ns/locn#>
PREFIX vcard: <http://www.w3.org/2006/vcard/ns#>

DELETE 
	{	?s ?rel ?addr .
		?addr ?p ?o }
WHERE
	{ 	?s ?rel ?addr .
		?addr ?p ?o .
		VALUES ?rel { vcard:hasAddress locn:address }
	}
