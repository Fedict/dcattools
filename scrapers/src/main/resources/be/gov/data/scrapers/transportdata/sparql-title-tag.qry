# Add default language tag for title

PREFIX dcterms: <http://purl.org/dc/terms/>

DELETE 
	{	?s dcterms:title ?str }
INSERT 
	{	?s dcterms:title ?streng }
WHERE {
		?s dcterms:title ?str
		BIND (STRLANG(?str, "en") AS ?streng)
		FILTER (LANG(?str) = "")
	}
